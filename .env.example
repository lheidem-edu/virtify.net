# Relay used for outbound mail (cancellation notices and their receipts).
# Port 25, no authentication — an internal relay that accepts mail from this
# server by address. Without it the cancellation form shows the declaration
# on screen and sends nothing.
VIRTIFY_SMTP_HOST=

# Optional: overrides the commit shown in the footer. Set this in container
# builds, where no .git directory is present.
# GIT_COMMIT_SHA=
